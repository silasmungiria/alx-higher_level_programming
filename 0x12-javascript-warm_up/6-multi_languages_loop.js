#!/usr/bin/node

// This line specifies the path to the interpreter for the script.

for (const line of ['C is fun', 'Python is cool', 'JavaScript is amazing']) {
  // This loop iterates over each element in the array,
  // ['C is fun', 'Python is cool', 'JavaScript is amazing'].
  // The "line" variable is assigned the value of each element in,
  // the array during each iteration.
  console.log(line);
  // This line outputs the value of the "line" variable to the console.
}
